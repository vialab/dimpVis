/**
 * Task descriptions to complete for the bar chart experiment,
 * dimpVis tasks are at index 0, time slider tasks are 1
 */
//Inner Array format: technique: [taskDescription,type(0 if single object, 1 if multiple objects), index(ices) of bar(s) involed in the task, solution]
var taskDescriptions = [
    [
        ["When is the orange bar at 20,000?",0,1,100]
    ], //Tasks for dimpvis

    [
        ["When is the orange bar at 20,000?",0,1,100]
    ], //Tasks for time slider

    [
        ["When is the orange bar at 20,000?",0,1,100]
    ] //Tasks for multiples
]